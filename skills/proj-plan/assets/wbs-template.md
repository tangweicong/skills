# WBS（工作分解结构）

> L1 可交付成果 / L2 子项；**阶段顺序与划分**见 `phase-roadmap.md`；L3+ 任务在 `phase-NN/plan.md` rolling 补充。

| 字段 | 值 |
|------|-----|
| 模式 | T / F |
| GATE-2 | |
| **在飞上限（WIP · ORD-52）** | **1**（同时处于「进行中」的 phase 数；可自定）|

> **中断协议（ORD-52 例外条款）**：允许插队，但被中断项**必须显式置「暂停」+ 在 `change-log.md` 记一行**（何时中断 / 被什么中断 / 恢复条件）。**禁止静默搁置**——不宣布就停，是停滞的头号成因。

## WBS 树

> **状态列仅 brownfield 接管项目填**（ORD-26 三态 `已完成/进行中/待做`）；greenfield 项目可省略该列。`已完成` = 既成约束，phase-roadmap / phase plan **不得**当新工作重新规划。来源列填 `DECISIONS` ID 或 handoff §条目。

| ID | 名称 | 完成定义（一句话） | 状态（brownfield）| 关联 DECISIONS / handoff |
|----|------|-------------------|------------------|--------------------------|
| 1.0 | | | 待做 / 进行中 / 已完成 | |
| 1.1 | | | | |
| 2.0 | | | | |

## 依赖

- …
