# 完整性审计报告 · {项目名}（分支 B · intent 不可重建）

| 字段 | 值 |
|------|-----|
| 触发 | GATE-S 判定 intent 不可信重建 → 仅审计 |
| 基线 | `docs/survey/{YYYY-MM-DD}-baseline.md` |
| 日期 | {YYYY-MM-DD} |

> **范围声明（S-2）**：无可信 intent/规格作参照，本报告**只评内部一致性**，**不**断言「无缺失 / 无 bug / 符合需求」。每条 finding 带**置信度**（高/中/低）。

## 审计维度与发现

| 维度 | finding | 置信度 | 依据 |
|------|---------|--------|------|
| 死代码 / 未接线模块 | … | | … |
| 未实现接口 / TODO/FIXME | … | | … |
| 测试覆盖与通过状态 | … | | 实跑结果 |
| 文档 ↔ 代码漂移 | … | | … |
| 已知反模式（资源泄漏/错误处理缺失/安全）| … | | … |

## 不能判定的部分

> 因缺 intent 而无法评估「是否符合预期」的点，显式列出，避免误以为"已审清"。

- …

## 建议下一步

- [ ] 若需继续 → 回 `proj-shape` 与人补 intent（确立 to-be）→ 再 `proj-plan`
- [ ] 若仅需修复明确 finding → 按置信度排序处理（终端）
