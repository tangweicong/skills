# 现状基线 · skills 仓库（EXP-05 dogfood 试跑）

| 字段 | 值 |
|------|-----|
| 目标对象 | 本仓库 `/Users/thh/workspace/self/skills/skills` |
| 生成方式 | AI 自动逆向盘点（**无人工整理**） |
| 生成日期 | 2026-06-01 |
| 真相源覆盖（ORD-24 优先级） | ✅ 测试/工具（validate_skills.py 实跑）· ✅ 代码（脚本/CI/pyproject）· ✅ git（log/status）· ✅ docs（README/CHANGELOG/CONTRIBUTING/DECISIONS）|
| 落地位置 | `docs/survey/`（**provisional · ORD-25 待确认**，可改） |
| 用途 | 验证 EXP-05：事实层误报率 + 分支判定与人判一致性 |

> **三分离标注**（复用 proj-shape 认知学，ORD-24）：**[事实]** = 直接读出/实跑可证；**[推理]** = 从结构/命名/状态推断（附依据）；**[待验证]** = 文档声称但本次未机验。
> **人工核对**：请逐条核对 **[事实]** 项,标出哪几条不成立 → 算误报率(见末节)。

---

## 一句话现状

[推理] 这是一个**已发布、基本稳定**的 Agent Skills 仓库——4 个 proj-* skill 已交付(对应 PMP 4 大 Process Group 的正向流水线),当前**唯一进行中的新增线** = brownfield 接管能力(proj-survey,设计阶段)。依据：EXP-01/02 passed + CHANGELOG 至 1.2.0 + DECISIONS 仅 proj-survey 线 `deciding`。

---

## 二、已完成范围(shipped / 既成事实)

| # | finding | 档 | 依据 |
|---|---------|----|------|
| F1 | 仓库含 **4 个已发布 skill**:`proj-experts` / `proj-shape` / `proj-plan` / `proj-run`(均在 `skills/<name>/SKILL.md`)| 事实 | `ls skills/` + README 索引表 |
| F2 | SKILL.md 行数:experts **191** · plan **302** · run **283** · shape **473** · template **29** | 事实 | `wc -l` |
| F3 | 校验脚本 `scripts/validate_skills.py` 存在;实跑 `uv run scripts/validate_skills.py` → `ok: 4 skill(s) validated`,**exit 0** | 事实 | 本次实跑 |
| F4 | 校验规则:`name` 必填/≤64/小写连字符/须与目录同名;`description` 必填/≤1024;SKILL.md >600 行告警;跳过 `template`/`_template` | 事实 | 读 `validate_skills.py` L42-63、L15 |
| F5 | CI `.github/workflows/validate.yml`:在 push(main/master)+PR 触发,跑 `uv sync` + validator | 事实 | 读 workflow |
| F6 | Python 工具项目:`pyproject.toml` name=`skills-repo-tools`,`requires-python>=3.11`,依赖 `pyyaml>=6.0`;有 `uv.lock` | 事实 | 读 pyproject + ls |
| F7 | assets 分布:**proj-experts 无 assets**(仅 SKILL.md)· proj-shape **3** 个(brainstorm/decisions/round)· proj-plan **21** 个 · proj-run **5** 个 | 事实 | `ls`/`wc` |
| F8 | `docs/discuss/DECISIONS.md` = 决定真源:`INV-01~04` + `ORD-01~26` + `EXP-01/02/04/05/06` | 事实 | 读 DECISIONS（ORD-23~26+EXP-05/06 = 本 session 09 轮新增）|
| F9 | git:分支 `main`,**7 commits**,**81 tracked files**;working tree 有未提交改动(`DECISIONS.md` modified + `09-*.md` untracked = 本 session 产物)| 事实 | `git log/status/ls-files` |
| F10 | CHANGELOG 版本线 `0.1.0 → 1.2.0`;最新 **1.2.0**(2026-05-28)= proj-shape 三段式入口 + 删除 `skills/README.md` | 事实 | 读 CHANGELOG |
| F11 | EXP-01/02 = **passed**;EXP-04 = **ABORTED with valuable insights**(cost 1.58x < 2x 阈值,其余 3 信号全过)| 事实 | DECISIONS + CHANGELOG |
| F12 | 仓库**自举(dogfood)**:用自身 proj-shape/proj-plan 流程开发——`docs/discuss/` 9 轮 + `docs/pmo/`(根 + proj-run-draft/)全套 PM artifact | 事实 | `ls docs/` + 内容 |
| F13 | `template/` 提供新 skill 骨架(29 行 SKILL.md),被 validator 跳过 | 事实 | ls + 读 SKILL.md + SKIP_DIRS |

---

## 三、未完成 / 进行中工作(从 DECISIONS 提取)

| # | finding | 档 | 依据 |
|---|---------|----|------|
| F14 | **唯一明确的未完成工作 = `proj-survey` skill**(brownfield 逆向盘点),状态 `deciding`,尚未建目录 | 事实 | DECISIONS ORD-23~26 标"待建/待落实" + 状态行 |
| F15 | proj-survey 卡在 2 个 pending 前沿验证:**EXP-05**(本基线即在跑)+ **EXP-06**(intent 重建 GATE 判据)| 事实 | DECISIONS EXP-05/06 |
| F16 | 4 项待确认未闭合:findings 落地目录 / 大 repo 分层读取 / baseline 刷新机制 / proj-plan WBS 三态 schema | 事实 | `09-…md` §待确认 |

---

## 四、质量 / 一致性观察

| # | finding | 档 | 依据 |
|---|---------|----|------|
| R1 | 无功能性 bug 迹象:唯一可执行代码(validator)实跑通过 + CI 在位;仓库主体是 markdown skill,无运行时 | 推理 | F3+F5;无运行时逻辑可崩 |
| R2 | **轻微文档/工具阈值不一致**:CONTRIBUTING 建议「SKILL.md 500 行以内」,validator 强制阈值是 **600** 行(proj-shape 473 行,两者都不触发)| 推理 | 读 CONTRIBUTING L9 vs validator L60 |
| R3 | proj-experts 无 assets 是**有意设计**,非缺失——README 明示其"可单独使用,不必开讨论文档"(纯对话 skill)| 推理 | README §proj-experts「单独使用」|
| R4 | proj-shape 473 行最接近 600 上限,后续扩展需留意搬 `references/` | 推理 | F2 + F4 |

---

## 五、待验证(文档声称 · 本次未机验)

| # | finding | 档 |
|---|---------|----|
| V1 | README 安装方式(`ln -s` 到 `~/.cursor/skills`)当前是否生效——未实际验证软链 | 待验证 |
| V2 | CHANGELOG 声称 proj-run 试跑 GATE 100% / analyze 7/7——历史声明,本次未重跑 | 待验证 |
| V3 | `docs/pmo/proj-run-draft/` 全套 artifact 的内部完整性——未逐一核对 | 待验证 |
| V4 | "4 skill 对应 PMP 4 大 Process Group"映射的准确性——属设计声明,非可机验事实 | 待验证 |

---

## 六、意图(to-be)重建评估 → 分支判定

| 维度 | 评估 |
|------|------|
| 意图来源丰富度 | **高**:README(项目意图)+ CHANGELOG(完整版本线)+ DECISIONS(INV/ORD/EXP 显式决定)+ 9 轮 discuss + dogfood PM artifact |
| 来源新鲜度 | **高**:最新 commit/CHANGELOG 2026-05-28;DECISIONS 本 session 刚更新 |
| 未完成工作可识别度 | **高**:proj-survey 线明确(ORD-23~26 + EXP-05/06)|
| 矛盾/陈旧 | 仅 R2 轻微阈值不一致,无方向性冲突 |

**分支判定 = 「可 plan」(intent 可信重建)。** 即若继续推进,应直接走 `proj-plan` 规划 proj-survey,**无需**降级到纯审计分支。

---

## 七、EXP-05 自检（待人工核对）

- **事实层条目数(分母)= 16**(F1–F16)
- **请你标出其中不成立/不准确的条目** → 误报率 = 错误数 / 16
- **建议成功阈值**:误报率 **< 10%**(即 ≤1 条错)→ 事实层可信;**分支判定(可 plan)与你的判断一致?** → EXP-05 success signal #2
- 若误报率高或分支判错 → 触发 EXP-05 中止/降级路径 B(人指认重点目录 + AI 局部分析)
