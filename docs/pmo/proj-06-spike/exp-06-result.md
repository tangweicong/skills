# EXP-06 结果（部分）· 分支判据算子化 + 区分力验证

| 字段 | 值 |
|------|-----|
| EXP | EXP-06（proj-survey §分支判据 · 去 provisional 的前置验证）|
| 日期 | 2026-06-29 |
| 假设 | 「intent 可信重建」存在**可操作**的 GATE 判据 |
| 成功信号 | 判据稳定可复现 + 分支判定**与人判一致** |
| 方法 | ①算子化 provisional 4 维（`criteria-operationalized.md`）②本仓库(易端)+合成难例 fixture 两端跑算子，验证区分力/可复现 |

## 判定：PARTIAL —「可操作 + 可复现 + 区分力」达标；「与独立人判一致(难端)」待真实语料

## 已验证（不依赖外部输入）

| 子目标 | 结果 | 证据 |
|--------|------|------|
| **判据可操作** | ✅（部分机判）| 4 维给出可观测信号 + 0/1/2 打分 + 确定性组合规则（R1/R2/R3）；组合规则**可机执行**（shell 跑出分支）。D1 源种类数 / D2 声称入口存在性 = **纯机判**；D3 缺口可定位 / D4 方向矛盾 = 信号客观但打分含**轻量语义判断** → 判据**部分算子化**，残余语义判断由 GATE-S 兜底 |
| **可复现** | ✅ | 本仓库客观信号（source_kinds=3 / 入口存在 / 待办体系明确 / 描述一致）→ D=2,2,2,2 total=8 → **PLAN**，复现 EXP-05「可 plan」结论 |
| **区分力（两端正确分开）** | ✅ | 合成难例（source_kinds=2 / 声称入口 server.py **缺失** / 仅 2 个 TODO 且注明原意不明 / README↔代码方向矛盾）→ D=1,0,0,0 → **R1 命中 → AUDIT**，与设计标签一致 |

**EXP-06 假设「存在可操作判据」→ 构造性成立**：至少 D1/D2 可纯机判，组合规则可机执行；难端经 R1 硬规则稳定落 AUDIT。

## 未达（需用户输入 · verifier≠maker 要求）

- **「与独立人判一致」在难端未验**：合成 fixture 的「audit」标签 = **我构造时的设计意图**，非独立人判 → 它验证的是**区分力/可操作/可复现**，**不是**独立一致性（否则 maker 自评，循环论证）。
- 「与独立人判一致」目前只在**易端**由 EXP-05 验证（本仓库 → plan，与用户人判一致）。
- **中间模糊带（R3 路径）无真实样本**：阈值 6/8 是可调超参，需真实难例校准。

## 待补（真实难例 + 独立人判）
要把 proj-survey §分支判据**去 provisional**，还差：在 1–2 个**真实** intent 难重建 repo 上跑算子，分支建议与**用户独立人判**对照。语料 + 人判须由用户提供（资产 + grader 中立性）。→ 见 GATE 提问。

## 安全性（即便判据未完美）
中止路径 B（R3 默认 AUDIT + GATE-S 人审批 · S-1）**已在 proj-survey 落地兜底**：算子只产**建议**，分支必经人审 → EXP-06 未闭合**不影响 proj-survey v1 安全可用**。
