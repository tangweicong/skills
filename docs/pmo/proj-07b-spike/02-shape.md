# 形态1 · 段2 shape（proj 调 proj-shape · 出沙盒决定）

> proj 跟随 proj-shape SKILL.md 收敛；**沙盒 ORD**（前缀 sROD 以示非 live 决定）。

## 沙盒决定
| ID | 决定 | 依据 |
|----|------|------|
| sROD-1 | 在 `DECISIONS.md` 顶部加**最小机读块** `pipeline-state`（字段：`stage` / `status` / `pending_exp`），仅作 CLASSIFY/VERIFY 索引，**不复制**散文细节（守 INV-03）| experts 视角 A∩B 收敛 |

## VERIFY（shape 段 · proj 跑）
- DECISIONS 同步检查：sROD-1 已记入本沙盒 → PASS（沙盒等价）。

## GATE（命中 → 默认档 STOP）
- shape 拟升 `ready-for-implementation`（sROD-1 要进入 plan）→ **默认档此处 STOP 交人显式确认**。
- 本 spike 在 EXP 授权下继续 → 标注后进 plan。
