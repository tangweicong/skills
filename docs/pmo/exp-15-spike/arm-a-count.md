# EXP-15 臂 A 计数（**锁定于臂 B 返回之前** · 2026-08-27）

> 本文件写于臂 B 结果返回**之前**，防止事后按臂 B 表现回调臂 A 的判定。计数规则见 `method.md`（同样预注册于两臂返回前）。
> 原始产出逐字存档：`arm-a-output.md`（父 agent 未编辑正文）。

## 信号 ①（跨视角显式反驳）= **0**

预注册三条件：(a) 点名另一视角 + (b) 针对其**具体结论** + (c) 表达**否定/对立**。

全文仅 2 处点名其他视角，**均不满足 (c)**：

| 位置 | 原句 | 判定 |
|------|------|------|
| 视角 B §选用理由 | 「**视角 A** 只管清单内容长什么样，不回答"持久化"这个动作的成本。」 | (a) ✓ 点名；(c) ✗ ——这是**范围互补**（"不回答 X"），预注册规则明确排除「说得对，但还要注意…」式补充。**不计** |
| Step 4 收敛节 | 「未收敛（留给人裁决，不做调和）：**视角 A** 认为 pause point 应绑在流程时点上…，**视角 B** 的派生视图逻辑其实允许任意时点按需重算…两者导出不同的 skill 写法」 | (a) ✓；(b) ✓ 具体结论；(c) ✗ ——措辞为「两者导出不同」而非否定，且**发出者是汇总方（Step 4），不是某视角在反驳另一视角**。**按 ① 严格判据不计** |

否定措辞机检（`我不同意|不同意视角|是错的|恰恰相反|该结论不成立|反驳`）：**0 命中**。

## ⚠ 必须记录的意外发现：臂 A **自发产出 1 条明确标注的未解决分歧**

Step 4 末尾那段「未收敛（留给人裁决，不做调和）」是**历史基线里没有出现过的行为**——r30 F5 实测 r13+r22 共 11 视角、全仓 28 轮**从未有过任何「分歧」章节**。

**这削弱（但不否定）干预的说服力**，必须在解读时带上。三条可能解释，本轮无法区分：

1. **议题差异**：J-3 本身的分歧度可能高于 r13/r22 那类评审型议题。
2. **需求特征（demand characteristic · 设计者引入的混淆）**：两臂 prompt 均含「being measured」字样，可能诱发比日常更"认真"的产出。**两臂同等受影响**，故不影响 A/B 对比；但**使臂 A 高于历史基线**，因此 ① 的达标线应以**本次臂 A 实测（0）**为准，不可宣称"相对历史基线的改善"。
3. 单次运行方差。

**推论**：现状格式**偶发**能暴露分歧，但**没有结构位**承载它（它挤在「收敛」节末尾，作为一个段落而非一等章节），且**没有任何视角真正反驳过任何视角**——F5 的核心结论（0 反驳）在本次臂 A 上**复现**。

## 信号 ②（证据源不重叠率）= **0% → "达标"** ⚠ **但该信号测量无效**

机检（`overlap.py arm-a-urls.json`）：三对两两交集全为 0，最大重叠率 0% ≤ 50% → 形式上达标。

**但臂 A 的检索独立性按设计为零**——它执行的是「**1 次共享框定 → 1 批共享事实 F1–F12 → 分发给 3 视角**」。之所以仍测出 0% 重叠，是因为三个视角各自**引用了同一批事实的不同子集**。

**结论：信号 ② 测的是「引用子集是否不同」，不是「是否独立检索」。** 一次检索产出 12 条事实、三视角各引不同子条，即可刷出 0% 重叠。

R-2 此前已将 ② 降为辅助（理由：证据异质不足以单独保证差异度）。本次发现**第二个更根本的理由：② 连"证据异质"本身都没测准**。→ 结果文档中须提出 ② 的重新操作化（建议改为测**检索行为**：各视角是否发起了各自独立的搜索批次，而非测引用集合）。

## 信号 ⑤（成本代理量）

臂 A 输出 **22,614 字符**；检索批次 **1 批**（覆盖 Q1–Q5，产出 F1–F12）。

## 信号 ③④（人判）

不由 AI 判定，交用户。臂 A 产物 = `arm-a-output.md`。

## 小结（臂 A）

| 信号 | 值 | 备注 |
|------|-----|------|
| ① 显式反驳 | **0** | F5 结论复现 |
| ② 证据不重叠 | 0%（形式达标）| **测量无效**，见上 |
| ⑤ 成本 | 22,614 字符 / 1 检索批次 | 基准 |
| 自发分歧段 | **1**（非 ① 口径）| 历史基线为 0，须计入解读 |
